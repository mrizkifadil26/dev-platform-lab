apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: vault
  namespace: vault
spec:
  virtualhost:
    fqdn: vault.localhost

  routes:
    - conditions:
        - prefix: /
      services:
        - name: vault
          port: 80