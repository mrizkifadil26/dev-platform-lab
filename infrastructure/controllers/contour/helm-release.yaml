---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: contour
  namespace: contour
spec:
  chart:
    spec:
      chart: contour
      interval: 1h0m0s
      version: ^0.5.0
      sourceRef:
        kind: HelmRepository
        name: contour
        namespace: flux-system
  interval: 1h0m0s
  timeout: 5m0s